# NLightning.Domain — agent guide

## Purpose
This is the pure domain layer: it holds value objects, models, wire-message models, and the interfaces that the outer layers implement. It uses the BCL only. There are **no** PackageReference or ProjectReference entries in `NLightning.Domain.csproj`. It targets net10.0 via `src/Directory.Build.props`. Serialization, crypto, persistence and DI all live in the outer layers.

## Layout (folder = namespace `NLightning.Domain.<Folder>...`)
- `Protocol/`: the wire model. It contains `Constants/MessageTypes.cs` (the ushort enum), `TlvConstants.cs`, `Messages/` (one sealed class per message), `Payloads/`, `Tlv/` (BaseTlv plus typed TLVs, and `TruncatedInt`: the one strict BOLT 1 tu16/tu32/tu64 codec, shared with the Infrastructure converters), `Models/TLVStream.cs` (the `TlvStream` class), `Models/CommitmentNumber.cs`, `Models/PerCommitmentIndex.cs`, `ValueObjects/` (BigSize, ChainHash, BitcoinNetwork) and `Interfaces/`. `Interfaces/` mixes the message contracts with key, secret, dust and transport services.
- `Channels/`: `Models/ChannelModel.cs` (aggregate), `Enums/ChannelState.cs`, `ValueObjects/` (ChannelId, ShortChannelId, Htlc, ChannelParams/ChannelParty, CommitmentKeys), `Factories/ChannelFactory.cs`, `Validators/ChannelOpenValidator.cs`, and the repository/manager interfaces. `Channels/Commitments/` is the pure BOLT 2 commitment state machine (see the section below).
- `Bitcoin/`: value objects (TxId, BitcoinScript, Witness), the `Transactions/` models, the `*ModelFactory` classes and the `*OutputInfo` types, plus ports (ILightningSigner, IFeeService, the Db/Memory repositories).
- `Crypto/`: CompactPubKey, PrivKey, Hash, Secret, CompactSignature, `CryptoConstants`, `ISha256` (hash through the one-shot `Sha256Extensions.ComputeHash`, which resets the state on failure) and `Interfaces/ISecp256K1Math` (EC tweak-mul/add port, implemented in Infrastructure.Bitcoin).
- `Protocol/Onion/`: BOLT 4 onion types (see the Onion routing section below).
- `Node/`: `FeatureSet.cs` (BOLT 9), `Options/FeatureOptions.cs` and `NodeOptions.cs`, and the peer interfaces. `NodeOptions.EnableHtlcs` is `bool?` (unset = on for regtest only); always read the effective `HtlcsEnabled`, which is computed on each read so it follows the network set in the daemon's `PostConfigure`. `NodeOptions.ReconnectInitialDelay`/`ReconnectMaxDelay` (default 5 s / 10 min) are the reconnect backoff knobs, and `NodeOptions.Routing` is `Options/RoutingOptions` (bound from `Node:Routing`: fee base (`uint`, u32 as in channel_update / BOLT 11 hints)/ppm, `CltvExpiryDelta` >= 34 (default 40), `MaxCltvExpiryDistance` 2016, `ExpiryTooSoonBlocks` 18, `InvoiceMinFinalCltvExpiry` >= 18 (default 40), invoice expiry, HTLC min/max). `NodeOptions.FeeUpdates` is `Options/FeeUpdateOptions` (`Node:FeeUpdates`: `Enabled`, `Interval` 10 min, `ThresholdPercent` 20, `NonAnchorFeerateMarginPercent` 200 (>= 100), `MinFeeratePerKw` >= 253 (`FeeratePerKwFloor`), `MaxFeeratePerKw` 50,000, `MaxAnchorFeeratePerKw` 2,500; used by the Application fee scheduler, N9-T1) and `NodeOptions.MaxDustHtlcExposureMsat` (`ulong?`, default 50,000 sat, null disables; BOLT 2 `max_dust_htlc_exposure_msat`, stored with each channel's first snapshot, NL-254). `NodeOptions.GetValidationErrors()` covers the reconnect delays, `Routing` and `FeeUpdates`; `FeatureOptions.GetValidationErrors()` stays separate, and the daemon's `ValidateOnStart` must call both (NodeServiceExtensions).
- `Payments/` (ABCD W0-C contracts; implementations come in later waves): `Models/` (`InvoiceModel`, `PaymentModel`, `ForwardCircuitModel` with forward-only status mutators and a validating `Restore` for persistence; `PaymentModel.Route` is the onion's route as `PaymentHop`s (node, scid, amount, CLTV, Sphinx shared secret; last hop = payee; optional ctor/`Restore` argument, empty when not recorded) so `HopSharedSecrets` can decrypt a failure after a restart; `ForwardingRequest`/`OutgoingChannelInfo`/`ForwardingDecision` for `IForwardingPolicy`; `PayInvoiceOptions` (per-call timeout, fee limit, part limit; NL-270) and `PayInvoiceResult` (the payment plus the call's HTLC attempts and most parts in flight) for the `IPaymentService.PayInvoiceAsync` overload that retries and splits), `Enums/` (persisted status bytes, never renumber), `ValueObjects/HtlcOrigin` (what an outgoing HTLC belongs to: `Local(paymentHash)` = kind 1 or `Forwarded(incomingChannelId, incomingHtlcId)` = kind 2; kind 0 is reserved so `default(HtlcOrigin).IsValid` is false and `OfferHtlcAsync` refuses it). `OfferHtlcAsync` saves the add and then returns the id, so the payment's/circuit's record of that id is a later save: a crash in between leaves a live HTLC whose circuit is still `Pending` (resolve it with `MarkFulfilled/MarkFailed(outgoingChannelId, outgoingHtlcId, at)`, valid from Pending) or whose payment has no `OutgoingHtlcId`; never fail either without first checking that no channel HTLC carries its origin. One stored payment per hash: a retry of a `Failed` payment replaces it, `Policies/ForwardingFee` (the BOLT 7 fee formula, 128-bit intermediate, rounded down) and `Interfaces/` (`IInvoiceDbRepository`, `IPaymentDbRepository`, `IForwardCircuitDbRepository` (staged writes, committed with `IUnitOfWork.SaveChangesAsync`; implemented in `Infrastructure.Repositories/Database/Payment/`, reached through `IUnitOfWork`), `IInvoiceService`, `IPaymentService`, `IForwardingPolicy`). Each interface's XML docs state its contract (persist before send, when it completes, which events resolve it).
- `Channels/Interfaces/IChannelOperations`: the only way payments/switch/IPC change a channel's HTLCs or feerate (offer/fulfill/fail/fail-malformed/update_fee/record onion secret). Contract in its XML docs: take the channel lock (callers hold none), check `HtlcsEnabled` + usable channel + engine rules (else `CommitmentRefusedException`, nothing persisted), persist the transition (and the `HtlcOrigin`) in one save, then enqueue; resolutions come back as channel domain events. The `FulfillHtlcAsync(..., Func<IUnitOfWork, Task> stageWithFulfill, ct)` overload stages the caller's writes (the final hop's invoice `Settled`, NL-253) in the fulfill's own save. `Exceptions/ChannelFailedException` (`FailedChannelId`, `MustBroadcast`, `RequirementId`) is the "send error and fail the channel" signal; the catcher persists `ChannelState.Failed` (35) and the error before sending it. Its `PeerMessage` is never blank (defaults to `DefaultPeerMessage`, "channel failed"). It derives from `ChannelErrorException`, so until N6-T3 adds a `catch (ChannelFailedException)` ahead of the generic handlers in PeerManager/ChannelManager, throwing it takes the old send-error-and-disconnect path without persisting Failed; don't throw it before then.
- `Channels/Policies/HtlcDeadlinePolicy` (BOLT2 plan N9-T2): pure BOLT 2 HTLC deadlines. `Evaluate(HtlcRecord, height, IncomingHtlcResolution)` → `HtlcDeadlineDecision(Action, RequirementId, DeadlineHeight)`: an offered HTLC in either commitment at `cltv_expiry + GraceBlocks` (2) → `FailChannel` (B2-CLTV-03); a received HTLC we fulfilled in either commitment, or locked in with its preimage known (`PreimageKnown`), at `cltv_expiry - FulfillSafetyBlocks` (18) → `FailChannel` (B2-CLTV-06); a locked-in, unremoved, `Unresolved` one at `cltv_expiry - FailBackBlocks` → `FailBackUpstream` (B2-FWD-03/CLTV-05); an `UnresolvedFinalHop` one (never forwarded, no preimage: the switch may still settle it) at the fulfillment deadline `cltv_expiry - FulfillSafetyBlocks` → `FailBackUpstream` (B2-CLTV-05; the forwarding distance would fail a payer's final HTLC, which is only about `min_final_cltv_expiry` away, almost at once); `AwaitingDownstream` never acts. `FailBackBlocks >= FulfillSafetyBlocks` is enforced. `ChannelSigningInfo.DataLossDetected` (from `ChannelModel.GetSigningInfo`) and `ILightningSigner.SignLocalCommitmentForBroadcast`/`MarkDataLoss` are the N9-T4 signer contract.
- `Money/LightningMoney.cs`: holds the amount in **msat**. `Enums/Feature.cs`: feature bits, each stored as its ODD bit number.
- `Serialization/Interfaces`, `Transport/` (`ITransport` is internal), `Persistence/Interfaces/IUnitOfWork.cs`, `Exceptions/`, `Utils/` (BitReader/BitWriter), `Client/` (IPC DTOs and the append-only `ClientCommand` enum: 9 CreateInvoice, 10 PayInvoice, 11 ListInvoices, 12 ListPayments, 13 CloseChannel, 14 ForceCloseChannel, 15 PendingSweeps, next free 16 (the BOLT 5 DTOs: `ForceCloseChannelClientRequest/Response`, `PendingSweepsClientRequest/Response` with `PendingSweepChannelInfo`/`PendingSweepOutputInfo`); invoice/payment responses map from the Payments models with `FromModel`; `ChannelInfoClientResponse` has `IsReestablished`, `FeeBaseMsat`, `FeePpm`), `Models/RoutingInfo*` (BOLT 11 hints).

## Dependency rules
- Do NOT reference NBitcoin, libsodium, EF Core, Microsoft.Extensions.* or any other src project. Keep the code library-free.
- Declare interfaces here and implement them outward: Infrastructure, Infrastructure.Bitcoin, Infrastructure.Serialization, Application.
- Internals are exposed through `AssemblyInfo.cs`, which lists Application, Infrastructure(.Blazor/.Bitcoin/.Serialization), Infrastructure.Serialization.Tests, Infrastructure.Tests, Tests.Utils and `DynamicProxyGenAssembly2` (Moq proxies). **`NLightning.Domain.Tests` is not in that list**, so Domain tests can only use the public API.
- Domain has no DI of its own. Its factories and validators are registered in `AddApplicationServices` (`src/NLightning.Application/DependencyInjection.cs`); the rest in each layer's `DependencyInjection.cs`, composed by `NodeServiceExtensions.AddNltgNodeServices`.

## Conventions
- Namespaces are file-scoped. The prevailing style puts project `using` lines *after* the namespace line, written relative to the enclosing namespace (for example `using Money;` inside `NLightning.Domain.Channels.Models`). System usings go above the namespace line. Some files (e.g. `Protocol/Tlv/RemoteAddressTlv.cs`, `Bitcoin/Transactions/**`) instead use fully qualified `using NLightning.Domain...` above the namespace.
- Byte-backed value objects are mostly readonly structs or record structs over `byte[]` (`CompactSignature` is a `record` class). They validate length in the constructor and provide implicit conversions to and from byte[]/Span/Memory. `Hash`, `TxId`, `ChainHash`, `CompactSignature` and `BaseTlv` compare content with `SequenceEqual`; `Secret` and `PrivKey` compare in constant time (`CryptographicOperations.FixedTimeEquals`). All hash with the null-safe `GetByteArrayHashCode()` (`Utils/Extensions/ByteArrayExtensions.cs`), so `default(...)` values hash to 0; `ShortChannelId` hashes its parsed fields. `Hash`/`Secret`/`TxId`/`PrivKey` require exactly 32 bytes. `CompactPubKey` converts implicitly from `ReadOnlySpan<byte>` (so `byte[]` still converts, as a copy) rather than from `byte[]`, so `cond ? null : key` is `CompactPubKey?` instead of a runtime NullReferenceException.
- Models are classes with private setters and `UpdateX`/`AddX` mutators. `AddX` throws if the value is already set.
- Protocol failures throw `ChannelErrorException` (connection is usually closed) or `ChannelWarningException`. Only the exception's `PeerMessage` property is sent to the peer (see `src/NLightning.Infrastructure/Node/Services/PeerCommunicationService.cs`); the `Message` stays local.
- Build amounts with `LightningMoney.Satoshis(..)` or `MilliSatoshis(..)`. A raw `ulong`/`long` converts implicitly as **msat**.
- Mark Constants classes and exceptions `[ExcludeFromCodeCoverage]`. Name tests `Given_X_When_Y_Then_Z` (xUnit + Moq).
- `dotnet format --verify-no-changes --exclude "**/BlazorTests/**"` is a CI gate. The `.editorconfig` rules are at error severity, including `_field`/`s_field` naming.

## Adding a wire message (most common change)
1. Add the value to `Protocol/Constants/MessageTypes.cs`.
2. Add `Protocol/Payloads/XPayload.cs`, implementing `IChannelMessagePayload` (or `IMessagePayload`).
3. Add `Protocol/Messages/XMessage.cs` as a `sealed class : BaseChannelMessage` with `public new XPayload Payload => (XPayload)base.Payload;`. Build `Extension = new TlvStream(); Extension.Add(tlvs...)` only when a TLV is non-null.
4. For new TLVs: add a `TlvConstants` entry, add `Tlv/XTlv.cs : BaseTlv`, add a converter in `src/NLightning.Infrastructure/Protocol/Tlv/Converters`, and register it in `TlvConverterFactory` (`TlvStreamSerializer` looks converters up by runtime type).
5. Add the payload serializer and message-type serializer, registered in both dictionaries of both `PayloadSerializerFactory` and `MessageTypeSerializerFactory`.
6. Add `Create*` to `Protocol/Interfaces/IMessageFactory.cs` and implement it in `src/NLightning.Application/Protocol/Factories/MessageFactory.cs`.
7. Route the message: add a `case` to the `switch` in `src/NLightning.Application/Channels/Managers/ChannelManager.cs` `HandleChannelMessageAsync` (~line 88) plus an `IChannelMessageHandler<T>` (`src/NLightning.Application/Channels/Handlers/Interfaces/IChannelMessageHandler.cs`). Without these, the `default` branch throws a channel-scoped `ChannelWarningException` (message ignored, `warning` sent, connection kept). Add a round-trip test in `test/NLightning.Infrastructure.Serialization.Tests/Messages`.

## Tests
- Unit tests: `dotnet test test/NLightning.Domain.Tests/NLightning.Domain.Tests.csproj` (878 pass; the 10k-seed simulator run is explicit, see below). Filter with `--filter "FullyQualifiedName~NLightning.Domain.Tests.ValueObjects.BigSizeTests"`.
- Wire-level coverage lives in `test/NLightning.Infrastructure.Serialization.Tests`. BOLT 3 vectors are in `test/NLightning.Integration.Tests/BOLT3` (run with `--filter 'FullyQualifiedName!~Docker'`).
- `test/NLightning.Domain.Tests` references `Microsoft.Extensions.Configuration.Binder`/`.Json` (test-only) so `Node/Options/NodeOptionsTests` can bind the `Node` section the way the daemon does; `src/NLightning.Domain` itself stays package-free.
- To match CI, build first: `dotnet build -c Release -p:MSBuildWarningsAsMessages=MSB4121`.

## Gotchas
- `TlvConstants` numbers are scoped per message: 0 and 1 are each reused several times. Use the semantically correct constant, and never mix TLVs from different namespaces in one `TlvStream`.
- For FeeRangeTlv, RemoteAddressTlv and FundingOutputContributionTlv, `Value` is not the wire bytes. Always go through the ITlvConverter.
- File name ≠ type name for `Models/TLVStream.cs`, `Tlv/NetworksTLV.cs` and `Tlv/RequireConfirmedInputsTLV.cs`.
- `ChannelModel.UpdateState` accepts only strictly increasing `ChannelState` values, and additionally forbids moving into `V2Opening` from any V1 state or leaving `V2Opening` downward. Number any new state with that in mind (current values: 0,1,2,3,10,20,21,22,23,25,30,35,37,40,50; `ShuttingDown = 23` and `Negotiating = 25` sit between `Open` and `Closing`, `Failed = 35` and `OnchainResolving = 37` (BOLT 5 plan O1-T2: a commitment is on chain and its outputs are being resolved; set by the Application `OnchainChannelWatcher` since ABCD W5) between `Closing` and `Closed`).
- Networks (ABCD W4-D): `BitcoinNetwork` names are lower-cased; `ChainHash` knows mainnet, testnet (testnet3; the constant had 4 garbled bytes until W4-D), regtest and signet (`ChainConstants.Signet`, genesis `00000008...1ef6` reversed) plus registered custom networks, and throws otherwise. Read configured names with `BitcoinNetwork.Resolve(name)`, which throws `ArgumentException` for an unknown name (never fall back to mainnet). Custom signets (Mutinynet, `NetworkConstants.Mutinynet`, registered by default; others through `Node:CustomSignet:Name` = `CustomSignetOptions.Register()` or the static `BitcoinNetwork.RegisterCustomSignet`) resolve to `Signet`, and `new BitcoinNetwork("mutinynet")` is `Signet` too: same chain hash, `tb` addresses, `tbs` invoices. `BitcoinNetwork.Register(name, chainHash)` is static (it was an instance method). The registries are process-wide; tests that register a name should `Unregister` it.
- `ShortChannelId(ulong)` uses the BOLT 7 widths (block 24 bits, tx index 24 bits, output 16 bits; NL-101).
- `CommitmentNumber(openerPaymentBasepoint, accepterPaymentBasepoint, sha256)`: BOLT 3 obscuring is SHA256(opener || accepter), so pass the funder's basepoint first regardless of which side is local (`ChannelDbRepository` reload orders them by `IsInitiator`, NL-127). It is an immutable per-channel **obscuring helper** with no value of its own: `Obscure(n)`, `LockTime(n)`, `Sequence(n)` (n is a 48-bit commitment number, `MaxValue`). The numbers live on the channel: `ChannelModel.LocalCommitmentNumber` / `RemoteCommitmentNumber` (both 0 after the open, independent of each other, never changed at funding confirmation; NL-188). `ICommitmentTransactionModelFactory.CreateCommitmentTransactionModel(channel, side, commitmentNumber)` takes the number explicitly and `CommitmentTransactionModel.Number` carries it. For `Remote` that convenience overload uses `RemoteKeySet.CurrentPerCommitmentCompactPoint` and throws `InvalidOperationException` unless `RemoteKeySet.CurrentPerCommitmentIndex` is the index of that number (the point is commitment 0's during funding and commitment 1's after channel_ready); build any other remote commitment with the `CommitmentTxSpec` overload and an explicit point.
- Commitment numbers vs per-commitment indices (NL-187): numbers count up from 0, BOLT 3 secret indices count down from 2^48-1. `PerCommitmentIndex.From(n) = 2^48-1-n` (and `ToCommitmentNumber`). Every `ILightningSigner` per-commitment API (`GetPerCommitmentPoint`, `RevealPerCommitmentSecret`, `AdvanceLocalCommitment`) and `ICommitmentKeyDerivationService.DeriveLocalCommitmentKeys` take **numbers**; only the signer converts to an index. `RevealPerCommitmentSecret(channelId, n)` is guarded: it throws unless `n < ` the signer's local commitment number (`ChannelSigningInfo.LocalCommitmentNumber` at registration, then `AdvanceLocalCommitment` after each persisted local commitment; NL-189). `CommitmentKeys` has no secret. HTLC signatures: `SignRemoteHtlcTransactions`, `ValidateLocalHtlcSignatures`, `SignLocalHtlcTransaction` over `Bitcoin/Transactions/Models/HtlcSigningContext` (NL-057); `ChannelSigningInfo.RemoteHtlcBasepoint` is needed for validation, and `ChannelModel.GetSigningInfo()` fills it and the local number. `CommitmentTransactionModel.PerCommitmentPoint` is the holder's point (set by the factory). channel_ready's second point is `GetPerCommitmentPoint(channelId, LocalCommitmentNumber + 1)`. `ChannelKeySetModel.CurrentPerCommitmentIndex` is still an index (remote side: updated by channel_ready). The peer's revealed per-commitment secrets have one source only: the shachain (`ISecretStorageService` Insert/Export/Load + `IRemoteShachainDbRepository`). `ChannelKeySetModel.LastRevealedPerCommitmentSecret` and its column were dropped by migration `AddCommitmentState` (NL-238).
- `ChannelModel.Commitments` (N5) holds the engine snapshot (`ChannelCommitments?`, null until the wiring creates one), plus `SentCommitDiff`, `LastSentCommitmentMessage` (`Channels/Enums`), `ErrorSent` and `DataLossDetected` (`MarkErrorSent`/`MarkDataLossDetected`). Swap it only with `UpdateCommitments(next, ChannelStateExtras?)` **after** the transition was saved (I2). While a snapshot is set, `LocalBalance`/`RemoteBalance`, the next HTLC ids and the commitment/revocation numbers are read from it. Build the engine's `CommitmentParams` with `CommitmentParams.FromChannel(channel, maxDustHtlcExposureMsat?)` only (it carries `HasInferredLimits`; the old `ChannelModel.ToCommitmentParams` dropped it and was removed). Persistence port: `Channels/Interfaces/IChannelStateDbRepository` (`InitializeAsync`, `ApplyAsync(next, transition, extras)`, `LoadAsync` → `Models/PersistedChannelState` with the settled-HTLC archive and the shachain, onion shared secret get/set, `PruneSettledHtlcsAsync`); `IUnitOfWork.ChannelStateDbRepository`. `IHtlcDbRepository` is gone, and the legacy `Htlc` collections on `ChannelModel` are no longer persisted (always empty after a reload).
- `ChannelModel.LocalBalance`/`RemoteBalance` are **gross**: they still include that side's own pending offered HTLCs. `CommitmentTxSpec.FromChannel` (`Channels/Commitments/CommitmentTxSpec.cs`) takes each HTLC out of its offerer's balance to produce the **net** spec (`ToLocalMsat`/`ToRemoteMsat` from the local node's view, `FeeRatePerKw`, `Htlcs`; `Outgoing` = offered by us). The future update_add_htlc flow must not deduct an HTLC at add time (it would be subtracted twice); move the amount only when the HTLC is fulfilled or failed.
- `CommitmentTransactionModelFactory` has two entry points: `(channel, side, number)` (spec from the channel, remote point from `RemoteKeySet.CurrentPerCommitmentCompactPoint`) and the spec-driven `(channel, CommitmentTxSpec, side, number, remotePerCommitmentPoint)` (N2-T2): the channel supplies only static data (keys, funding output, obscuring factor, dust limits, to_self_delay, anchors, funder). The remote side **requires** the point, the local side rejects one (its point comes from the signer by number). Fees and trimming go through the static `Bitcoin/Transactions/Factories/CommitmentFeeCalculator` (724/1124 + 172 per untrimmed HTLC, HTLC fees 663/703 or **0 with anchors**, funder pays fee + 2x330 anchors, amounts rounded down). The factory never checks the channel reserve (NL-196; that is an update-validation rule); `ChannelOpenValidator` rejects an open_channel whose initial to_local and to_remote are both <= channel_reserve_satoshis (NL-220). The model carries `FeeRatePerKw`, `HasAnchors`, `ToSelfDelay`, `LocalDelayedPubKey`, `RevocationPubKey` for building HTLC transactions.
- `FeatureSet.DeserializeFromBytes` copies its input (it used to reverse the caller's array in place, which also scrambled `ChannelTypeTlv.Value` for multi-byte channel types). `new FeatureSet()` sets 5 compulsory bits: data_loss_protect, var_onion_optin, static_remote_key, payment_secret, channel_type. BOLT 9 dependencies and per-context masks (`Enums/FeatureContext`: I/N/C/9/B/T) live in `FeatureSet` (`GetDependencies`, `GetContexts`, `FilterByContext`); `FeatureOptions.GetNodeFeatures(context)` filters (default `Init`) and `GetValidationErrors()` is enforced at daemon start. Dependencies on ASSUMED features are deliberately not encoded, except zero_fee_commitments -> option_channel_type (still listed by BOLT 9). `IsCompatible` checks both sets' dependencies, treats ASSUMED features (0, 8, 12, 14, 44) the peer omits as set optional, and records a feature as Compulsory if either side requires it and Optional if both only support it, so callers' `negotiatedFeatures.X == Compulsory` branches are live. `ChannelTypeTlv` holds big-endian wire bytes: build it with `new ChannelTypeTlv(featureSet)` or `featureSet.GetWireBytes()`, never `GetBytes()` (little-endian).
- `ChannelModel.ChannelParams` (NL-194) holds `Local` (the `ChannelParty` values **we** announced) and `Remote` (the peer's), plus shared feerate, minimum depth, anchors and scid-alias use. Direction: `Local.HtlcMinimum/MaxAcceptedHtlcs/MaxHtlcValueInFlight` bind HTLCs the peer offers us; `Local.ChannelReserveAmount` is what the **peer** must keep; our commitment uses `Local.DustLimitAmount` and `Remote.ToSelfDelay`, the peer's uses `Remote.DustLimitAmount` and `Local.ToSelfDelay` (`CommitmentTransactionModelFactory`). The initiator's `Remote` is `ChannelParty.Unknown` until accept_channel (`UpdateRemoteParams`). `ToChannelType()` is the channel_type the initiator sends; the non-initiator echoes the opener's bytes. `ChannelOpenValidator` accepts only channel_type bits 12, 22, 46, 50 (and 46 only when scid_alias was negotiated). Upfront shutdown scripts live on the parties (`ChannelModel.Local/RemoteUpfrontShutdownScript` just forward).
- `FeatureSet.GetBytes` used to drop a highest set bit at a multiple of 8 (e.g. channel_type bit 40); fixed. `GetSetBits()`/`HasSameBits()` compare bit sets regardless of bitmap length.
- `LightningMoney` is a mutable reference type (NL-202; the commitment engine uses checked `ulong` msat instead). The `-` operator throws on underflow.
- The `protected internal BaseMessage(MessageTypes)` constructor installs `PlaceholderPayload`, whose `ChannelId` throws. `Stfu`, `Error` and `Warning` messages are not `IChannelMessage`.

## Onion routing (BOLT 4): M1+M2 done, M3 error onions done, M3b attribution_data done (not wired into the switch)
Everything is under `Protocol/Onion/` (namespace `NLightning.Domain.Protocol.Onion.*`) plus `Exceptions/OnionException.cs`; tests in `test/NLightning.Domain.Tests/Protocol/Onion/`.
- `Constants/OnionConstants` (1366/1300/32 sizes, version 0, key labels `Rho`/`Mu`/`Um`/`Pad`/`Ammag`/`AmmagExt`/`BlindedNodeId`/`Fulfillment`, error-packet limits, `MaxHtlcCltv`), `OnionPayloadTlvTypes` (2..18, a separate namespace from `TlvConstants`), `EncryptedDataTlvTypes` (route-blinding namespace, M5; constants only).
- `ValueObjects/OnionPacket`: raw version(1) + pubkey(33) + hop_payloads(variable) + hmac(32). It validates only lengths, never the version byte or the pubkey, so `update_add_htlc` still parses and the peeler can return `invalid_onion_version`/`invalid_onion_key`. `default(OnionPacket)` members throw.
- `Models/OnionHop` (node id + raw payload TLV bytes **without** the bigsize length prefix), `ConstructedOnion` (packet + per-hop shared secrets), `PeeledOnion` (raw `Payload`, `SharedSecret`, `NextPacket` or null when final, `PathKeySharedSecret`), `HopPayload` (TLV stream with nullable typed accessors; unknown odd records kept).
- `Tlv/*Tlv` (AmtToForward, OutgoingCltvValue, OnionShortChannelId, PaymentData, EncryptedRecipientData, CurrentPathKey, PaymentMetadata, TotalAmountMsat). Converters live in Infrastructure (`Protocol/Tlv/Converters/Onion/`).
- `Enums/FailureCode` (all BOLT 4 codes, plus the legacy PERM|16 `IncorrectPaymentAmount` and 17 `FinalExpiryTooSoon` (no data; recognised when received, never sent) so a final-node 17 is understood and retryable), `FailureCodeFlags`, `Extensions/FailureCodeExtensions` (`IsBadOnion()` etc.), `Enums/OnionPacketKind` (Payment: payload length >= 2; OnionMessage: >= 0).
- `Interfaces/ISphinxService` (construct/peel; implemented by `SphinxService` in Infrastructure.Bitcoin), `Interfaces/IOnionReplayStore` (NL-078: the HMAC replay set owned by the incoming HTLC and expired by its `cltv_expiry`; the node's `PersistentOnionReplayStore` and the in-memory `InMemoryOnionReplayStore` are in Infrastructure; `IncomingOnionProcessor` records through it with a `Models/OnionReplayOwner(channelId, htlcId, cltvExpiry)`), `Interfaces/IOnionReplayDbRepository` (its table `OnionReplayEntries`, rows `Models/OnionReplayEntry`; `Add` staged, `DeleteExpiredAsync` immediate; `IUnitOfWork.OnionReplayDbRepository`), `Interfaces/IFailureOnionService` (M3 error onion: create/wrap/decrypt; implemented by `FailureOnionService` in Infrastructure.Bitcoin).
- `Models/FailureMessage` (M3): `Code`, `Data` (exactly the code-specific data, no code/TLV), `Extension` (raw `BaseTlv` records; no failure TLV types are defined, so there is no `FailureTlvTypes`). The ctor validates `Data` against the BOLT 4 layout of every defined code (fixed fields, `u16 len || channel_update` for UPDATE codes, canonical bigsize for `invalid_onion_payload`); unknown codes keep all bytes as data. One static factory per code (`TemporaryNodeFailure()`, `AmountBelowMinimum(htlcAmount, channelUpdate = default)`, `IncorrectOrUnknownPaymentDetails(htlcAmount, height)`, ...; `channel_update` defaults to empty, `len = 0`), `WithExtension`, typed accessors (`HtlcAmount`, `CltvExpiry`, `Height`, `DisabledFlags`, `ChannelUpdate`, `Sha256OfOnion`, `InvalidPayloadType/Offset`; `null` for other codes) and `TryGetDataLength(code, span)` (splits data from trailing TLVs; used by the serializer). `Models/DecryptedFailure`: `ErringHopIndex`, `RawMessage` (the framed failuremsg), `Message` (null if unparsable) and `Code` (falls back to the raw first two bytes).
- `Serialization/Interfaces/IFailureMessageSerializer` (sync; implemented in Infrastructure.Serialization): failuremsg <-> `FailureMessage`, and the return-packet body framing `u16 failure_len || failuremsg || u16 pad_len || pad`.
- `Protocol/Tlv/BigSizeCodec` (internal): span-based canonical bigsize read/write shared by `InvalidOnionPayloadFailureFactory` and the failure serializer.
- `Validators/HopPayloadValidator` (static, pure): BOLT 4 reader rules for a parsed payload given `isFinalHop` and `hasUpdateAddPathKey`. Always reports `invalid_onion_payload`; the `invalid_onion_blinding` remap for blinded routes is the caller's (M4/M5) job.
- `Factories/InvalidOnionPayloadFailureFactory`: encodes/decodes the `bigsize type || u16 offset` failure data and creates the `OnionException`.
- `Exceptions/OnionException : ErrorException`: `FailureCode`, `FailureData` (sha256_of_onion for BADONION codes, type/offset for `invalid_onion_payload`) and `SharedSecret` (set when the failure must go back encrypted in `update_fail_htlc`).
- Malformed conversion (M3-T3): `FailureMessage.FromMalformed(code, sha256OfOnion)` (throws unless the BADONION bit is set; unknown BADONION codes are kept, data = sha256_of_onion, as BOLT 2 says) and `Validators/MalformedHtlcValidator.Validate(failureCode, sha256OfOnion, sentOnionSha256)` -> `Enums/MalformedHtlcCheckResult` (`BadOnionBitNotSet` = BOLT 2 warning+close or fail channel; `Sha256OfOnionMismatch` = hash differs and is not all zero: MAY retry/alternate error; an all-zero hash, e.g. `invalid_onion_blinding` from a blinded path, is `Valid` and MUST be converted). The packet itself is `IFailureOnionService.CreateErrorPacketFromMalformed(incomingSharedSecret, code, sha256OfOnion)`.
- channel_update in UPDATE failures: `Factories/FailureChannelUpdateFactory` (`Encode(payload)` writes `u16 258 || payload`, as LND/CLN/Eclair/LDK do; BOLT 4 itself is silent on the type prefix; empty payload -> empty field, `len = 0`, which BOLT 4 now prefers; `TryGetPayload(field)` strips an optional type prefix; `TryReadHeader(payload)` reads scid + timestamp). `FailureMessage.WithChannelUpdate(bytes)` replaces the field of any UPDATE failure (keeps fixed fields and TLVs); `HasChannelUpdateField`. Typed overloads (W0-E): `Encode(ChannelUpdateMessage)` (= `u16 258 || Payload.GetBytes()`, byte-identical to the wire serializer) and `TryGetChannelUpdate(field, out ChannelUpdateMessage?)` (signature not checked; a field starting with 0x0102 is always read as prefixed, so ~1/65536 bare-format updates with trailing fields are misread: verify the signature).
- BOLT 7 `channel_update` (258) is typed (W0-E): `Payloads/ChannelUpdatePayload` holds the fields (raw `ulong`/`uint` msat values, not the mutable `LightningMoney`; the signature and `ExtraData` arrays are copied in and `Signature` returns a copy, so a signed payload cannot change), `ExtraData` (unknown trailing fields, covered by the signature), flag constants/accessors (`MessageFlagMustBeOne`, `MessageFlagDontForward`, `ChannelFlagDirection`, `ChannelFlagDisable`; `Direction`, `IsDisabled`, `DontForward`) and is the single codec of the body: `GetSignedData()` (everything after the signature), `GetSignatureHash()` (SHA256d of it, BCL `SHA256`), `GetBytes()`, `Parse`/`TryParse`. Sign with `EmptySignature` -> `ILightningSigner.SignNodeMessage(GetSignatureHash())` -> `WithSignature(sig)`; verify with `ILightningSigner.VerifyNodeMessage`. `ChannelUpdateMessage` is a plain `BaseMessage` (no longer a raw `GossipMessage`); `PeerService` raises it through `IPeerService.OnChannelUpdateReceived` and the Application `IChannelUpdateService` checks and stores it (W1-E). Tests: `Protocol/Payloads/ChannelUpdatePayloadTests.cs`, `Protocol/Onion/FailureChannelUpdateFactoryTests.cs`.
- Origin interpretation: `Interpreters/FailureInterpreter.Interpret(DecryptedFailure?, routeLength)` -> `Models/FailureInterpretation` (`ErringHopIndex`/`IsAttributed`, `IsFinalNode`, `IsPermanent`, `IsNodeFailure`, `ShouldRetry`, `ExcludedNodeHopIndex` for NODE failures at intermediate hops, `FailedChannelHopIndex` = erring + 1 (the route hop reached through the failing outgoing channel), `ChannelUpdate` payload for UPDATE failures: use only for retrying this payment, never apply to the graph). Final node: PERM or not understood -> fail the payment. Implementation choices (not in BOLT 4): an intermediate failure with no readable code blames the erring node (temporary); an unattributable one (null) blames nobody and allows a retry.
- Attribution (M3b, ABCD W6-D): `Interfaces/IAttributionDataService` (implemented by `AttributionDataService` in Infrastructure.Bitcoin) creates/wraps/decrypts a failure with its `attribution_data` (`AttributedErrorPacket(Reason, AttributionData)`, `AttributedFailure(Failure, Attribution)` with `BlamedHopIndex` = the legacy erring hop, else the first hop whose attribution HMAC failed), and for `update_fulfill_htlc` `CreateFulfillment` (final node; optional `fulfillment_payload` records, padding added), `WrapFulfillment` (intermediate), `WrapFulfillmentPayload` (blinded hop: payload only) and `VerifyFulfillment` (origin) → `VerifiedFulfillment(Attribution, FulfillmentPayloadStatus None/Valid/Invalid, PayloadRecords)`. `AttributionVerification` (`IsPresent`, `HoldTimes` per verified hop in route order, `InvalidHopIndex`; `Absent`). Hold times are u32 in 100 ms units (`AttributionHoldTime.FromDuration/ToDuration`). Constants: `OnionConstants.Attribution*` (20 hops, 4-byte hold times and HMACs, 210 HMACs, 920 bytes), `MaxFulfillmentPayloadLength` 32768, `FulfillmentPayloadTlvTypes.Padding` (1, own namespace). Wire: `Tlv/AttributionDataTlv` (TLV 1, exactly 920 bytes) on `UpdateFailHtlcMessage` and `UpdateFulfillHtlcMessage`, `Tlv/FulfillmentPayloadTlv` (TLV 3, `IsTooLong` > 32768: BOLT 2 says send `error` and fail the channel, so the serializer accepts it and the handler must check); `TlvConstants.AttributionData`/`FulfillmentPayload`. A node whose incoming add had a `path_key` adds no attribution. `OptionAttributionData` stays experimental-gated until the switch uses the service.
- Not yet: `ChannelModel` HTLC mutators (BOLT2 N5/N6; the engine in `Channels/Commitments/` holds the HTLC state today), `onion_message` (513) in `MessageTypes` (M6). The HTLC state values exist now (`Channels/Enums/HtlcState`, see the commitment state machine section).
- Raw wire hooks: `Payloads/UpdateAddHtlcPayload.OnionRoutingPacket` is a mandatory `ReadOnlyMemory<byte>` of exactly `OnionConstants.PacketLength` (the constructor enforces it); parse it with `OnionPacket`. `UpdateFailHtlcPayload.Reason` is opaque; `UpdateFailMalformedHtlcPayload.FailureCode` is a bare ushort. `Tlv/BlindedPathTlv` carries the update_add_htlc path_key.
- Feature bits: VarOnionOptin(9, always compulsory), PaymentSecret(15), BasicMpp(17), OptionRouteBlinding(25), OptionAttributionData(37), OptionOnionMessages(39), OptionPaymentMetadata(49). `IPeerService.SendMessageAsync` only accepts `IChannelMessage`; `SendGossipMessageAsync` takes BOLT 7 gossip (256-265). `IPeerService.WaitForInitAsync` completes once the peer's init was accepted (NL-240: `PeerManager` installs a connection only after it).
- `FeatureOptions` defaults `OptionRouteBlinding` and `OptionAttributionData` to No; turn them on only when M5/M3b land. Unimplemented features are listed in `FeatureOptions.ExperimentalFeatures` (anchors, quiesce, dual_fund, route_blinding, attribution_data, simple_close, basic_mpp, onion_messages, provide_storage): they are never advertised and fail `GetValidationErrors()` (so the daemon refuses to start) unless `AllowExperimentalFeatures` is set (`Node:Features:AllowExperimentalFeatures`); `GetNodeOptions` (negotiated features) sets it so negotiated bits are kept. Remove a feature from the set when it is implemented. Other unimplemented features (gossip_queries_ex, basic_mpp, dual_fund, quiesce, provide_storage, scid_alias) also default to No; upfront_shutdown_script defaults to No until we generate a local script and shutdown enforces the peer's; data_loss_protect is advertised Optional (not Compulsory) and gossip_queries stays Optional (see NL-100).

## BOLT 5 on-chain building blocks (plan `docs/agents/BOLT5_ONCHAIN_PLAN.md`): `Onchain/`
Pure, BCL only; tests in `test/NLightning.Domain.Tests/Onchain/` (the Appendix C vector runs are in `test/NLightning.Infrastructure.Bitcoin.Tests/Onchain/`). `Onchain/Interfaces/` (ports) belongs to the wiring lanes.
- Resolver port (ABCD W5, O2-T5): `Interfaces/IOutputResolver` (`CanResolve(ChannelCloseKind)`, `ResolveAsync(close, outputs, height, ct)` every block and at startup, `OnOutputSpentAsync(close, output, ChainTx spender, height, ct)` when a watched resolution output is spent) returns `Models/OutputResolverAction`s: `UpsertOutputAction`, `BroadcastAction` (a `BroadcastTransactionModel`, fully signed), `WatchOutpointAction`, `RaiseChannelEventAction` (an `IChannelDomainEvent` for the HTLC switch), `StageWriteAction` (any other staged write, e.g. a preimage before its fulfill), `AlertAction`. The Application executor applies one round's actions in one save, then publishes, watches, raises and alerts; it marks spends `Resolved`, makes them `Irrevocable` at depth 100 and closes the channel itself, so resolvers only decide. Resolvers keep no state and repeat actions until their effect is on chain. `Models/OutputDescriptorData` is the `OutputResolutionModel.DescriptorData` blob the watcher writes (version 1: amount, CSV, anchors, scriptPubKey, witness script, the commitment holder's point, the `SpecHtlc`; never keys): `FromDescriptor`, `Encode`, `Decode` (throws `FormatException`), `TryDecode(row)`. `BroadcastTransactionModel.CommitmentNumber` (a `LocalCommitment` row's number, NL-271/NL-297), `MarkAbandoned()`, `MarkReplaced()` (pending only, O6-T1 RBF) and `MarkPending()` (abandoned/replaced only, O6-T3: what superseded it was reorged out); `IBroadcastTransactionDbRepository.GetByChannelIdAsync`/`MarkAbandonedAsync`/`MarkReplacedAsync`/`MarkPendingAsync`.
- `Models/ChainTx` (`TxId`, version, raw locktime, inputs `(PreviousTxId, PreviousVout, Sequence, Witness items)`, outputs `(AmountSat, ScriptPubKey)`; `IndexOfInputSpending`): a transaction seen on chain, built by `Infrastructure.Bitcoin/Onchain/ChainTxMapper` (txids in internal byte order, as `TxId` everywhere).
- `Protocol/Models/CommitmentNumber.Decode(lockTime, sequence)` → `ulong?` (null without the 0x20/0x80 prefixes) and static `TryGetObscured`: the inverse of `LockTime`/`Sequence` (Appendix C: `0x2bb038521914 ^ 42` decodes to 42). Local and remote commitments share the factor, so a number never tells whose commitment it is.
- `Classifiers/FundingSpendClassifier.Classify(ChainTx, FundingSpendContext)` → `FundingSpendClassification(Kind, CommitmentNumber?, TxIdMatched, Reason)`; `Enums/FundingSpendKind` (persisted byte: Unknown 0, Mutual 1, LocalCommit 2, RemoteCommit 3, RemoteNextCommit 4, Revoked 5, FutureRemote 6, NotFundingSpend 7). The context holds the funding outpoint, the obscuring helper, `CommitmentCandidate(Number, TxId)` for our latest local, the peer's current and its unacked next commitment (txids from the mapper's rebuild), our closing txids and both shutdown scripts. Order: not spending the funding outpoint → NotFundingSpend; known closing txid → Mutual; txid equal to a candidate → that candidate; more than one input or no commitment prefixes → Mutual if an output pays a shutdown script, else Unknown (B5-GEN-06); then by number with the txid unmatched: next → RemoteNextCommit, current → RemoteCommit (rebuild differs: map by script), below → Revoked, above → FutureRemote (data loss). A non-matching commitment is always the peer's (our local ones need our funding signature, which the signer gives only for the latest, I4/S1). Never throws for a malformed spender.
- `Enums/CommitmentCase` (Local 1, Remote 2, Revoked 3), `Enums/OutputDescriptorKind` (persisted byte: Unknown 0, DelayedToLocal 1, LocalOfferedHtlc 2, LocalReceivedHtlc 3, PaymentToRemote 4, RemoteReceivedHtlc 5 (our offered HTLC on their commitment), RemoteOfferedHtlc 6, RevokedToLocal 7, RevokedHtlc 8, PeerOutput 9, OurAnchor 10, PeerAnchor 11), `Models/CommitmentOutputDescriptor` (vout, amount, kind, scriptPubKey, witness script (null for P2WPKH), `SpecHtlc?` from our point of view, CSV delay (to_self_delay on to_local, 1 on anchor HTLC/to_remote outputs, 16 on anchors), `HasAnchors`, and for our own HTLC outputs the `HtlcTransactionModel` of the HTLC-timeout/success that spends them; `IsOurs`) and `Models/CommitmentOutputMap` (case, number, holder's per-commitment point, rebuilt and on-chain txids, `TxIdMatched`, descriptors in vout order, `HtlcsWithoutOutput` (trimmed or missing, B5-LCL-LO-04), `UnmappedVouts`). Keys are never stored in a descriptor; they are re-derived from the point.
- `Parsers/HtlcWitnessParser`: `Parse(witness)` → `HtlcSpendWitness(Enums/HtlcSpendPath, Secret? preimage candidate)` from the witness shape (5 items `0 sig sig <32|empty> script` = HTLC-success/timeout tx; 3 items `sig <32|empty|33> script` = preimage claim / timeout claim / revocation); `TryExtractPreimage(witness | (ChainTx, spent txid, vout), paymentHash, out Secret)` reports a preimage only when `SHA256(preimage) == payment_hash` (BCL `SHA256.HashData`). Never throws.
- Sweeps (O3-T1, O4, O5-T1): `Enums/SweepKeyKind` (DelayedPayment 1, Payment 2, HtlcRemotePoint 3, Revocation 4: the four signer keys) and `Enums/SweepSpendKind` (DelayedOutput 1 `<sig> <>` with nSequence = CSV, PaymentToRemote 2 P2WPKH `<sig> <basepoint>` or anchors `<sig>` with CSV 1, HtlcTimeoutClaim 3 `<sig> <>` with nLockTime = cltv_expiry, HtlcPreimageClaim 4 `<sig> <preimage>`, RevokedDelayedOutput 5 `<revsig> 1`, RevokedHtlc 6 `<revsig> <revocationpubkey>`; `SweepSpendKinds.GetKeyKind()`/`IsPenalty()`), `Models/SweepInput` (outpoint, amount, spend kind, witness script (null only for P2WPKH to_remote), CSV, cltv, point / peer secret / preimage / the pubkey the witness pushes), `Factories/SweepInputFactory` (`ToLocal`, `SecondLevelOutput`, `ToRemote`, `HtlcTimeoutClaim`, `HtlcPreimageClaim`, `Penalty`, `SecondLevelPenalty`; each checks the descriptor kind it resolves), `Models/SweepSigningContext` (unsigned tx, input index, witness script, amount, key kind, point, secret: the `ILightningSigner.SignSweepInput` contract) and `Models/UnsignedSweepTransaction` (built tx, inputs, destination, output/fee, estimated weight; `GetSigningContext(i)`).
- `Fees/SweepWeights`: witness size per spend kind with 73-byte signatures (exact for the real script: offered HTLC penalty 243, accepted 249 with a 3-byte cltv push (248 with Appendix C's 2-byte expiries), to_local penalty 155/156, below BOLT 5's 160 which assumes an 8-byte delay), the BOLT 5 constants (324/407/413, overhead 4*53+2, to_remote +272), `EstimateTransactionWeight`, `FeeSat` (rounded down), `VirtualSize`. `Fees/SweepFeePolicy` (O6-T1, pure; `SweepFeePolicyOptions`: floor 253, sweep target 36, max target 144, safety 3, RBF interval 2, x1.25, 1 sat/vB relay, caps 50 % sweep / 100 % penalty within `SecurityDelay` 18): `GetConfirmationTarget(tip, deadline?)`, `ApplyFloor`, `GetMaxFee`, `Decide(value, weight, estimate, isPenalty, tip, deadline?)` → `SweepFeeDecision(rate, fee, capped, abandon)` (abandon = value <= fee at the floor), `ShouldBump`, `GetReplacementFee` (BIP 125 rules 3/4), `ShouldSplitPenalty` (B5-REV-08), `GetRevokedOutputDeadline` (to_local: conf + to_self_delay; their HTLC: cltv; ours: next block), `RbfSequence` 0xFFFFFFFD, `DecideReplacement(value, oldFee, weight, estimate, isPenalty, tip, deadline?, dust)` (O6-T1: max(BIP 125 minimum, fee at the estimate), capped by `GetMaxFee` and value − dust; null when even the BIP 125 minimum does not fit). The caller passes the estimate in: `IFeeService.GetFeeRatePerKwAsync(confirmationTarget, ct)` (NL-296; a default interface method answering the node-wide rate, so fakes need no change) for `GetConfirmationTarget`.
- `Planners/OutputResolutionPlanner` (O3-T2, O4-T2, O5-T2; pure and stateless, re-run on every block): `Plan(CommitmentOutputDescriptor, OutputResolutionFacts)` → `Models/OutputResolutionPlan(Enums/PlannedResolutionState (Unresolved/Resolved/IrrevocablyResolved in BOLT 5 terms; distinct from the persisted `OutputResolutionState` workflow byte), ResolutionAction[])` with `Enums/ResolutionActionKind` (Wait until a tip height, BroadcastHtlcTimeoutTx, BroadcastHtlcSuccessTx, Sweep with a `SweepSpendKind` on the commitment output or on the second-level output, RaiseFulfilled, RaiseFailed, AlertLostFunds), each tagged with its `B5-*` requirement and, for penalties/claims, a deadline height. Facts: tip, commitment height, the confirmed `OutputSpend` (txid, height, ByUs, `HtlcSpendPath`, checked preimage) and the second-level spend, the allowed preimage (never an invoice preimage the switch did not accept, B5-LCL-RO-02), whether the peer is irrevocably committed (B5-LCL-RO-03), whether the upstream event was already raised, the second-level CSV, reasonable (6) and irrevocable (100) depths. Heights: a CSV spend is broadcast once `tip + 1 >= confirmed + csv`, a CLTV spend once `tip >= cltv_expiry`. Upstream events are only for HTLCs we offered: fulfill as soon as a preimage is known, fail once the settling transaction is `ReasonableDepth` deep. `PlanHtlcWithoutOutput(SpecHtlc, HtlcWithoutOutputFacts)` covers B5-LCL-LO-04/B5-RMT-LO-03/B5-REV-RES-03. Tests: `test/NLightning.Domain.Tests/Onchain/OutputResolutionPlannerTests.cs` (one region per requirement group).

## Cooperative close (BOLT 2 plan N10): `Channels/Closing/`
- `ShutdownScriptValidator` (static): the BOLT 2 `shutdown` forms (`IsValid(script, anySegwit, simpleClose)`: P2WPKH, P2WSH, segwit v1-16 with a 2-40 byte push only with anysegwit, single-push OP_RETURN 6-80 bytes only with simple_close) and the BOLT 3 dust thresholds per script (`GetDustThresholdSat`: 546/540/294/330/240/354, OP_RETURN 0, unknown forms 546).
- `LegacyClosingTransactionFactory.Create(funding, localMsat, remoteMsat, localIsFunder, feeSat, localScript, remoteScript, dustLimitSat, ClosingVariant)` → `Bitcoin/Transactions/Models/ClosingTransactionModel` (outputs rounded down to sat, fee from the funder, outputs below the signer's dust limit removed, `WithoutLocalOutput`/`WithoutRemoteOutput` variants; throws for a fee above the funder's balance or no output left). `ClosingFeeCalculator`: `EstimateWeight(localScriptLen, remoteScriptLen)` (724 for P2WPKH + P2WSH, like BOLT 3's commitment) and `FeeSat(feerate, weight)`.
- `LegacyClosingNegotiator` (pure): `Open(state, idealFee)` (the funder's first proposal, clamped into `ClosingNegotiation.Acceptable`, with that range when `SendFeeRange`) and `Receive(state, fee, theirRange?, idealFee)` → `ClosingDecision` (Propose / Agree with or without the echo / Warn / Fail, each tagged with its B2-CLS row) + the next `ClosingNegotiation`. Receiver order: equal to our last fee (R02), inside our sent range (R03), then with their range: no overlap → warning (R04), funder takes a fee in the overlap and echoes it or fails (R05), non-funder that already proposed fails on another fee (R06) else proposes the received fee clamped into the overlap; without their range: strictly between our last and their previous fee or warning + close (R07), inside `Acceptable` → echo (R08), else the midpoint clamped into `Acceptable`, strictly between; when our last fee already is our bound we re-send it (a deviation from the R09 SHOULD, so a peer that moves in small steps, like LND's 10 % per round, reaches it) and give up with a warning + close after `MaxRounds` (100) received messages (R09). A peer that sends no `fee_range` switches our side to no range too (mixing the two rule sets can fail the channel through R05). The state is memory only (B2-RE-29). Property tests: 200 seeds each converge within 64 messages without ranges and 3 with ranges.
- `ChannelCommitments.IsCleared`: no HTLC, all fee updates final, no unacked remote commitment (the BOLT 2 point where closing negotiation starts). `ChannelModel.LocalShutdownScript`/`RemoteShutdownScript`/`ClosingTransaction` with `Set*` mutators (a shutdown script never changes once set). `Channels/Interfaces/IChannelCloseService` (`CloseChannelAsync(channelId, ChannelCloseRequest(feerate?, sendFeeRange, waitFor?))` → `ChannelCloseResult(channelId, state, closingTxId?)`), implemented in Application.

## Reestablish (BOLT 2 plan N7): `Channels/Reestablish/`
- `ReestablishPlanner` (static, pure): `CreateOwn(ReestablishLocalState)` → `OwnReestablish` (next_commitment_number L+1, next_revocation_number R, the peer's secret number R-1 or none, our point number L); `Plan(local, PeerReestablish, isOurSecret)` → `ReestablishPlan` (`Resume` with ordered `ReestablishStep`s: TxAbort, ChannelReady, RevokeAndAck/CommitDiff in `LastSentCommitmentMessage` order, UnsignedUpdates; `Fail` with the §6.9 requirement id and `MustBroadcast` for next_commitment_number 0; `DataLoss`, B2-RE-23). Checks in order: X = 0; Y > L (data loss when S is our secret Y-1, else fail); S must be our secret Y-1 (zeroes for Y = 0; the plan text said L-1, which is wrong when our last revoke_and_ack was lost); Y ∈ {L-1 (resend), L}; X ∈ {R+1 (resend the stored diff if we have an unacked commitment), R+2 with an unacked one}; X = 1 and L = 0 → channel_ready. `isOurSecret` must compare against our point (never reveal). `ReestablishLocalState.From(commitments, hasSentCommitDiff, lastSent)`; `HasUnsignedLocalUpdates` = our states 10/35 or our unsigned fee update.
- `IReestablishTracker.IsReestablished(channelId)`: whether a channel can carry updates on its peer's current connection (implemented by Application's `ReestablishTracker`; for `listchannels`).
- `IChannelManager` lifecycle (N7): `OnPeerConnectedAsync(peer)` → stored errors of failed channels, `OnPeerDisconnectedAsync(peer)`, `OnPeerConnectionChanged(peer)` (sync); `HandleChannelMessageAsync` returns `Task` (NL-234). `IPeerService.SendErrorAsync(ErrorMessage)` sends an `error` without disconnecting.

## Commitment state machine (BOLT 2 plan N4): `Channels/Commitments/`
Pure engine (no I/O, persistence or crypto), namespace `NLightning.Domain.Channels.Commitments`; tests in `test/NLightning.Domain.Tests/Channels/Commitments/`. Not wired into handlers yet (N5 persists it, N6 wires it).
- `Channels/Enums/HtlcState` holds core-lightning's `htlc_state`: 10-19 (we offered), 30-39 (they offered). Legacy values 0-3 (`Offered/Fulfilled/Failed/Expired`) still exist for old rows and are rejected by the machine. `HtlcStateTable` is the only place transitions live: `Next(state, HtlcEvent)` (throws `HtlcStateTransitionException : ChannelErrorException`), `TryNext`, and the flags `IsInLocalCommit/IsInRemoteCommit/IsRemovedFrom/IsRemoval/IsFinal/IsAddIrrevocablyCommitted/Owner`. Six events only: SendCommit, RecvCommit, SendRevoke, RecvRevoke, SendRemove, RecvRemove.
- `ChannelCommitments` (not `Commitments`, which would clash with the namespace) is an immutable `sealed record` snapshot: settled balances (msat, `ulong`, checked; open HTLCs stay in the offerer's balance, `Local + Remote == funding`), `Htlcs` (`ImmutableSortedDictionary<HtlcKey, HtlcRecord>`, key = direction + id), `FeeUpdates` (same state set, owner = funder; a commitment's feerate is the last update in it, a final one prunes older ones), next HTLC ids (start at 0), `LocalCommit`, `RemoteCommit`, `RemoteNextCommit` (unacked; blocks signing, D7) and `RemoteNextPerCommitmentPoint`. Build with `Create(...)` (after open) or `Restore(...)` (from persistence; validates states, owners, ids and balances).
- Every operation returns `CommitmentsResult { Next, Outbound, Transition, Events }` and never mutates the receiver: `SendAdd/ReceiveAdd`, `SendFulfill/SendFail/SendFailMalformed`, `ReceiveFulfill/ReceiveFail/ReceiveFailMalformed` (preimage checks take an `ISha256`), `SendFee/ReceiveFee(feerate, min, max)`, `SendCommit(ICommitmentSigner)`, `ReceiveCommit(CommitmentSignatures, ICommitmentVerifier)` (also performs our revoke and returns an `OutboundRevokeAndAck(revokedNumber, nextNumber)` **placeholder**: the caller derives the secret only after persisting, I3), `ReceiveRevoke(secret, nextPoint, IRevocationVerifier)`, `RevertUncommitted()` (disconnect). `Outbound` items are engine records (`OutboundAddHtlc`, `OutboundCommitmentSigned`, ...), not wire messages. `ChannelTransition` lists upserted/settled/dropped HTLCs and which parts changed, for a single save; `ReceiveRevoke` also sets `RevokedRemoteCommit` (the peer commitment it revoked, null for every other operation), which the persistence layer writes to the revocation log in the RAA's save (BOLT 5 plan O1-T1).
- Domain events (N4-T4, invariant I8): `Channels/Commitments/Events/` holds `IChannelDomainEvent` (`ChannelId`, `HtlcId`) and five records. `IncomingHtlcLockedIn(channelId, HtlcRecord)` is raised exactly once, by the `ReceiveRevoke` that moves an incoming HTLC 33 -> 34. `OutgoingHtlcFulfilled(channelId, id, hash, preimage)` is raised by `ReceiveFulfill` at once, only the first time the preimage is learnt (a fulfill re-sent after `RevertUncommitted` raises nothing, because `KnownPreimage` survives). `OutgoingHtlcFailed(channelId, id, hash, HtlcRemoval)` is raised only by the `ReceiveRevoke` that makes an outgoing fail/fail-malformed final (state 19), never earlier. `OutgoingHtlcSettled(channelId, id, hash, kind)` is raised for every outgoing HTLC that reaches 19 (after `OutgoingHtlcFailed` for a failure). `IncomingHtlcSettled(channelId, id, hash, kind)` (ABCD W2-B, NL-243) is raised by the `ReceiveCommit` whose revoke makes our removal of an incoming HTLC final (state 39), so the switch can prune its archived row. Within one result, events are ordered by HTLC key. `ChannelDomainEvents.DerivePending(channelId, records)` / `DerivePending(commitments, unprunedSettled)` derives the still-pending events from persisted records with the same rules (incoming in 34 -> locked-in; incoming in 39 -> incoming settled; outgoing with a known preimage -> fulfilled; outgoing in 19 -> failed and/or settled). It is the startup replay: pass every open record plus the settled records not yet pruned. Consumers must therefore be idempotent. The engine's events are computed by the internal `ChannelDomainEvents.FromChange(before, after, settled)` in `Result`, so every raised event is re-derivable from the transition it came with (the simulator checks this after every step). `Channels/Interfaces/IHtlcSwitch.HandleAsync(IChannelDomainEvent, ct)` is the consumer port (no implementation yet; N6-T2).
- Local rule breaks throw `CommitmentRefusedException` (never sent to the peer); peer rule breaks throw `CommitmentViolationException : ChannelErrorException` (`MustFailChannel` for B2-RAA-R01). Both carry `RequirementId` = the `B2-*` row of the plan matrix. Update checks live in `Channels/Validators/UpdateValidator` (internal) and use "prospective" views: adds count as soon as they exist, removals only once committed in that commitment, feerate = max(current, latest pending). Receiver checks judge only what the peer provably knew when it sent the update (both directions cross): `update_add_htlc` affordability (B2-ADD-R02) and the R03 counts leave out our adds the peer has not signed yet (states 10-12) and count our removals it has acked (state 37, credited to the offerer on fail, the receiver on fulfill) as done (`peerView`, matching LND's sender view), and `update_fee` (B2-FEE-R03) is checked on our current `LocalCommit`, as BOLT 2 words it. Because these are lenient, `ReceiveCommit` makes the delayed check ("MAY delay this check until the update_fee is committed"): when the peer is the funder, the new local commitment must leave it enough for `FunderCostMsat`, else a violation (B2-FEE-R03 if the commitment carries a new feerate, else B2-ADD-R02) before any signature is verified or anything revoked. B2-ADD-S04 (non-funder sender) checks the funder's fee on both commitments ("the updated local or remote transaction"); the fee-spike buffer (B2-ADD-S03) is also applied to both; the non-funder reserve refusal is B2-ADD-S01. The two receiver fixes came from the two-engine simulator (see below).
- `HtlcRecord.KnownPreimage` holds the preimage the peer revealed with `update_fulfill_htlc` for an HTLC we offered. Unlike `Removal` it survives `RevertUncommitted` (B2-RE-04: a reverted fulfill resets the HTLC to 14 with `Removal = null` but keeps the preimage), so N5 must persist the preimage column from `KnownPreimage` (never from `Removal` alone) and must not clear it when writing an upserted row.
- `RevertUncommitted` must run on every disconnect **and** after every `Restore` before `channel_reestablish`: a restored state-30 add is otherwise kept, and `ReceiveAdd` treats the peer's compliant re-send of that id as B2-ADD-R07 (the BOLT 2 "ignore a repeated id after reconnection" path is N7's).
- BOLT 3 fees and trimming have one implementation (NL-231): `Bitcoin/Transactions/Factories/CommitmentFeeCalculator`. The `…Satoshis` members hold the formulas (ulong), the `LightningMoney` members serve the transaction factories, and the `CommitmentSpec` members (`UntrimmedHtlcCount`, `TrimmedHtlcTotalMsat`, `CommitmentBaseFeeSatoshis(spec, …)`, `FunderCostMsat`, `IsHtlcTrimmed(spec, htlc, …)`) serve the engine and `UpdateValidator`; all delegate to the same formulas, so the engine's `num_htlcs` and fees can never diverge from the built transaction. Spec-number tests: `test/NLightning.Domain.Tests/Channels/Commitments/CommitmentFeeCalculatorSpecTests.cs` (engine view) and `Bitcoin/Transactions/CommitmentFeeCalculatorTests.cs` (factory view). `CommitmentFees` no longer exists.
- Invariant harness (plan N4-T5): `test/NLightning.Domain.Tests/Channels/Commitments/CommitmentPairSimulator.cs` drives two engines over FIFO links with seeded random add/fulfill/fail/malformed/fee/CS/RAA, deliveries and disconnects (`RevertUncommitted` + BOLT 2 retransmission computed from the `channel_reestablish` numbers) and checks the engine-level invariants after every step, then settles against an independent ledger. 500 seeds run by default; 10k seeds are an explicit `Category=Long` test: `dotnet test test/NLightning.Domain.Tests -c Release --filter "Category=Long" -- xUnit.Explicit=only` (~5 s). A failure names its seed; add it to `CommitmentSimulatorTests.Given_Seed_When_Replayed_Then_InvariantsHold` with the fix. One outcome is expected rather than a failure: when a funder add/`update_fee` crossed non-funder adds and the commitment it then signs cannot pay its fee, Bob's commit-time check fails the channel (a real protocol race; LND fails too); the run stops and counts it in `CrossedFeeFailures` (6 in the 10k run). The invariants also check that every local commitment of the non-funder pays its fee (the funder's own commitment can fall short in that race). Any change to the engine that touches update/commit/revoke must keep both green.
- Seam with the N2/N3 builder and signer (NL-230): the engine uses `CommitmentSide` from `Bitcoin/Transactions/Enums` (one enum for both). There is one crypto port family, the engine's `Channels/Commitments/Interfaces/{ICommitmentSigner,ICommitmentVerifier,IRevocationVerifier}` (channel id + `CommitmentSpec` / `CommitmentSignatures`); the old `Channels/Interfaces/ICommitmentSigner`/`ICommitmentVerifier` (channel + `CommitmentTxSpec`) were deleted. The Application adapters `EngineCommitmentSignerPort`/`EngineCommitmentVerifierPort`/`EngineRevocationVerifierPort` implement them over `CommitmentSigningService` and `IPerCommitmentSecretVerifier`. Seam helpers: `CommitmentTxSpec.FromCommitmentSpec(spec)` (both are net, local-node views, so balances and HTLCs map one to one; the holder becomes the factory's `CommitmentSide`; the built `Htlc`s carry only amount/hash/expiry/direction/id, `AddMessage` is null because the builders never read it; `Htlc.AddMessage` is nullable since NL-244), `CommitmentParams.FromChannel(channel, maxDustExposureMsat)` (funder, funding sats, anchors, both `ChannelParty` values with the same direction rules, and `ChannelParams.HasInferredParams` as `CommitmentParams.HasInferredLimits`: for NL-194 migrated channels `UpdateValidator.ValidateReceiveAdd` skips the checks against our guessed htlc_minimum / max_accepted_htlcs / max_htlc_value_in_flight and the peer's guessed reserve; amount 0, cltv and fee affordability still apply, and send-side checks stay (they only refuse)) and `CommitmentTxSignatures.ToCommitmentSignatures()`. The builder input `CommitmentTxSpec` and the engine's `CommitmentSpec` stay separate types.
- A received `commitment_signed` with no updates is accepted (BOLT 2 only forbids sending one). `update_fee` affordability on receive ignores the reserve (BOLT 2 does not mention it); on send it keeps our reserve.
